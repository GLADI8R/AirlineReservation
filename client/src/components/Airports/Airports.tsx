import { Fragment } from "react";


const Airports = () => {
    return (
        <Fragment>
            <h1>Airports</h1>
            {/* displays results */}
            

            
            <button>Add Airport</button>
        </Fragment>
    );
};

export default Airports;