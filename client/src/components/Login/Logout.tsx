


const Logout = () => {

    const logout = (e:any) => {
        
    }
    return(
        <>
            <h1>Logout</h1>
            <hr/>
            <button onClick={logout}>confirm</button>
        </>
    );
}

export default Logout;